import React from 'react'

const ModalRoot = document.getElementById('modal-body')

class Modal extends React.Component {
  constructor(props) {
    super(props)
    this.el = document.createElement('div')
  }
}